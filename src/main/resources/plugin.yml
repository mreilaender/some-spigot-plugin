name: ${project.artifactId}
version: ${project.version}
author: mreilaender
main: org.example.www.ExampleMain
prefix: example

commands:
  example:
    description: Example command
    usage: /example